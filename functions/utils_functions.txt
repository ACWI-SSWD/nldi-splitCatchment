geom_to_geojson( geom, name )
	return geojson_dict

transform_geom( proj, geom)
	return projected_geom

get_local_catchment( x, y )
	return catchmentIndentifier, catchmentGeom

get_local_flowlines( catchmentIndentifier )
	return flowlines, nhdFlowline

get_upstream_basin( catchmentIndentifier )
	return upstreamBasinGeom

merge_geometry( catchment, splitCatchment, upstreamBasin )
	return mergedCatchmentGeom

get_coordsys()
	return transformToRaster, transformToWGS84

project_point( x, y, transformToRaster )
	return projected_xy

get_flowgrid( catchment_geom, transformToRaster, transformToWGS84 )
	return flw, flwdir_transform

split_catchment(flw, flwdir_transform, projected_xy, transformToWGS84)
	return split_geom

get_onFlowline(projected_xy, nhdFlowline, flowlines, transformToRaster, transformToWGS84 )  
	return onFlowline

get_raindropPath( flw, projected_xy, nhdFlowline, flowlines, transformToRaster, transformToWGS84 )
	return downstreamPath

get_intersectionPoint(x, y, onFlowline, raindropPath)
	return intersectionPoint

get_reachMeasure( intersectionPoint, flowlines, *downstreamPath )
	return streamInfo

split_flowline(intersectionPoint, flowlines)
	return upstreamFlowline, downstreamFlowline
